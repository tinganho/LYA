
syntax = "proto3";

package lya;

service Lya {
    rpc sync (SyncRequest) returns (SyncResponse);
}

message SyncRequest {
    repeated string function_names = 1;
    repeated string file_names = 2;
}

message SyncResponse {
    repeated string localizations = 1;
}